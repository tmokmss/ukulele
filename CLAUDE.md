# このリポジトリでの約束

## 実装が終わったら

- **PR を作るのと同時に、ローカルの開発サーバー (`npm run dev`) も起動して URL を伝える**
  - すぐ触って確かめられるところまでが1セット。PR のリンクだけ渡して終わりにしない
  - 起動はバックグラウンドで。ポートが埋まっていれば別のポートでよく、実際に出た URL を伝える
  - ワークツリーで作業したときは、そのワークツリーから起動する (PR のブランチが動く)
  - 依存が入っていなければ `npm ci` してから。止め方も一緒に伝える
